<h2>Definition for the word: <%= @word.name %></h2>

<br>
  <% if @word.definitions %>
    <ul>
      <% @word.definitions.each do |definition| %>
        <li><a href="/words/<%= @word.id %>/definitions/<%= definition.id %>"><%= definition.name %></a></li>
      <% end %>
    </ul>
  <% else %>
    <p>There are no definitions for this word yet!.</p>
  <% end %>

<h4>Add a definition:</h4>

<form action="/words/<%= @word.id %>/definitions" method="post">
  <div class="form-group">
    <label for="definition_name">Definition:</label>
    <input id="definition_name" name="definition_name" class="form-control" type="text" placeholder="Create a new definition here!" required>
  </div>
  <button type="submit" class="btn">Add definition</button>
</form>

<p><a href="/words/<%= @word.id %>/edit">Edit Word</a></p>
<a href="/words">Return to word list</a>